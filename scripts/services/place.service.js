//TODO: export relevant functions

let gPlaces = get(entityType, entityId) || 

function createPlaces(){
//TODO: make an array gPlaces with all created places
//TODO: store gPlaces to storage
}

function _createPlace() {
    return {
        id,
        name,
        lat,
        lng,
        weather,
        createdAt: Date.now(),
        updatedAt
    }
}